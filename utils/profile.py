import sqlite3

database = sqlite3.connect('data/database.db')
c = database.cursor()

def favGenre(user): #returns user's favorite genre


def makeGenre(id, user): #allows user to change favorite genre


def favMovie(user): #returns user's favorite movie


def makeMovie(id, user): #allows user to change favorite movie
    
